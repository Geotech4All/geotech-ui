import {
  MOST_LISTENED_PODCASTS,
  RECENT_HOSTS,
  PREVIOUS_GUESTS,
  STAFF_LIST } from "@gql/requests/queries/AdminQueries";

import { ALL_PODCASTS } from "@gql/requests/queries/ClientQueries";

export {
  STAFF_LIST,
  RECENT_HOSTS,
  ALL_PODCASTS,
  PREVIOUS_GUESTS,
  MOST_LISTENED_PODCASTS,
}
