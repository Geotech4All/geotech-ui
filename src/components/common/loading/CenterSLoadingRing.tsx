import React from "react";
import { SLoadingRing } from "@components/common";

export default function CenterSLoadingRing() {
  return (
    <div className="flex items-center justify-center w-full min-h-[5rem]">
      <SLoadingRing />
    </div>
  )
}
