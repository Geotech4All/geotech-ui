import { adminUrls } from "@constants/adminUrls"
import { dummyPosts } from "@constants/clientContants";
import { images } from  "@constants/images";
import type { UrlType } from "@constants/adminUrls"
import * as Hooks from "@constants/hooks"

export { adminUrls, dummyPosts, Hooks, images };
export type { UrlType }
