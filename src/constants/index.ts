import { adminUrls } from "@constants/adminUrls"
import type { UrlType } from "@constants/adminUrls"
import { podcastColors } from "@constants/podcastColors";
import type { PodcastColorType } from "@constants/podcastColors";
import * as Hooks from "@constants/hooks"

export { adminUrls, podcastColors, Hooks };
export type { UrlType, PodcastColorType }
